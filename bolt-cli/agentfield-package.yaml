config_version: v1
name: "bolt-cli"
version: 1.0.0
description: "Run the bolt-cli TypeScript AgentField scaffold locally."
author: "Devflex-ai"
language: typescript
entrypoint:
  start: npm run start
  healthcheck: /health
agent_node:
  node_id: "bolt-cli"
  default_port: 8001
user_environment:
  required: []
